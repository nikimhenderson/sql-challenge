departments
-
dept_no VARCHAR(4) PK FK -< dept_emp.dept_no
dept_name VARCHAR(50)

dept_emp
-
emp_no INTEGER PK FK - dept_manager.emp_no
dept_no VARCHAR(4) FK >-< dept_manager.dept_no

dept_manager
-
dept_no VARCHAR(4) 
emp_no INTEGER PK

employees
-
emp_no INTEGER PK FK - salaries.emp_no
emp_title_id VARCHAR(5) FK >- titles.title_id
birth_date DATE
first_name VARCHAR(50)
last_name VARCHAR(50)
sex VARCHAR(1)
hire_date DATE

salaries
-
emp_no INTEGER PK FK - dept_manager.emp_no
salary INTEGER

titles
-
title_id VARCHAR(5) PK
title VARCHAR(30)
